import {
    Divider as MantineDivider,
    DividerProps as MantineDividerProps,
} from '@mantine/core';

export const Divider = (props: MantineDividerProps) => {
    return <MantineDivider {...props} />;
};
